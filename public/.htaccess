RewriteEngine On

# Sirve archivos/directorios reales
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Todo lo demás a index.php (Slim)
RewriteRule ^ index.php [L]
